# Vite 8's optional Vitest 5 peers crash npm's arborist
# (`Cannot read properties of null (reading 'edgesOut')`) when
# regenerating the lockfile against Vitest 4. Keep installs on
# the patched Vitest 4 tree until that resolver bug is gone.
legacy-peer-deps=true
